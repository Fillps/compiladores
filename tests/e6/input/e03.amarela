/* recursion:  in the bss address */


int fatorial (int n)
{
    if (n == 1) then {
       return n;
    };
    return fatorial(n-1) * n;          
}

int main()
{
  int x;
  int y;
  y = 6;
  x = fatorial(y);
  return 0;
}
